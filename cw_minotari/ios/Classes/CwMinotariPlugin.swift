import Flutter
import UIKit

public class CwMinotariPlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {
    // No method channel needed - Flutter Rust Bridge handles FFI directly
  }
}
